interface exploreCardProps{
    children?: React.ReactNode;
    title?: string;
    discription?: string;
    link?: string;
    onClick?: React.MouseEventHandler<HTMLDivElement>;
}

export default function Exportcard({children, title, discription, link, onClick}: exploreCardProps){
    return(
        <div>

        </div>
    );
};